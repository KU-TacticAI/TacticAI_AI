# FastAPI 웹 프레임워크
fastapi==0.104.1

# ASGI 서버 (FastAPI 실행용)
uvicorn[standard]==0.24.0

# RabbitMQ 클라이언트
aio-pika>=9.5.5
# aio-pika로 변경: 비동기 RabbitMQ 클라이언트
# pytest-asyncio는 테스트를 async로 작성할 때 필요할 수 있음
# pytest-asyncio>=0.21.0

# 데이터 검증 및 직렬화 (FastAPI에서 사용)
pydantic==2.5.0

# 환경변수 관리 (.env 파일 로드)
python-dotenv==1.0.0

# HTTP 클라이언트 (선택적, 향후 확장용)
httpx==0.25.2

# 로깅 및 유틸리티
python-multipart==0.0.6
